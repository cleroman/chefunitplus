import 'package:flutter/material.dart';


class AppColors {
  // --- Primaire : Mauve ---
  static const mauve       = Color(0xFF7B4B94);
  static const mauveLight  = Color(0xFFA57BB8);
  static const mauveDark   = Color(0xFF4A2A5E);
  static const mauveSoft   = Color(0xFFEFE7F3);

  // --- Secondaire : Kaki ---
  static const kaki        = Color(0xFF8B8B4A);
  static const kakiLight   = Color(0xFFC9C9A0);
  static const kakiDark    = Color(0xFF5C5C30);

  // --- Statuts ---
  static const success     = Color(0xFF2E7D32); // Vert
  static const successSoft = Color(0xFFC8E6C9);
  static const danger      = Color(0xFFC62828); // Rouge
  static const dangerSoft  = Color(0xFFFFCDD2);
  static const warning     = Color(0xFFF9A825); // ambre (pending)

  // --- Neutres ---
  static const background  = Color(0xFFF5F3F7);
  static const surface     = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1C1B1F);
  static const textMuted   = Color(0xFF6E6A72);
  static const divider     = Color(0xFFE0DCE5);

  // --- Dark mode ---
  static const darkBg      = Color(0xFF1A1020);
  static const darkSurface = Color(0xFF251A2B);
}